# Data Maintainer View

This feature is not routed directly. It is intended to be included as a subcomponent or embedded view elsewhere in the app.

- Remove `index.ts` and `data-maintainer-view.routes.ts` if present.
- Use `DataMaintainerViewComponent` as a presentational or embedded component.
