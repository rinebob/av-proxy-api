// Re-export the mock data service
export { mockDataService } from './mock-data.service';

// Re-export types for consistency
export * from '../../common/common-dm';

// Re-export the registration function
export { registerAllMockData } from './register-mock-data';

// Re-export the mock data for direct imports if needed
export { MOCK_COMPANY_OVERVIEW_DATA } from './company-overview.mock';
